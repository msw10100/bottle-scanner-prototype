# Elixir version
elixir_version=1.14.5

# Erlang version
erlang_version=25.3

# Always rebuild from scratch on every deploy?
always_rebuild=false

# Create a release using `mix release`? (recommended)
runtime_path=/app